
import React, { Component, PropTypes } from 'react'
import styles Alias.css';


export default Alias extends Component {
  constructor(props){
    super(props)

    this.state = {
        currentstate: stateValue
    }
    //this.changeBackground = this.changeBackground.bind(this)
  }
    //changeBackground(evt){
        //this.setState({
          //currentstate  = evt.target.value
        //})
      //}


  //AHORA ESTO NO IMPORTA
  //static propTypes = {
    //mobile: PropTypes.boolen.isRequired
  //}
  //static defaultProps = {
    //mobile: false
  //}


  render() {
      // Js functions and JS conditions using this.props.nameProp

    return (
      <div>
        {/* <input type=text value={this.state.currentstate} onChange={changeBackground}/> */}

      </div>
    )
  }
}
